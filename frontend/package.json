{
  "name": "tank-simulator-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "set NODE_PATH=..\\node_modules && vue-cli-service serve",
    "build": "set NODE_PATH=..\\node_modules && vue-cli-service build",
    "lint": "set NODE_PATH=..\\node_modules && vue-cli-service lint"
  },
  "workspaces": {
    "nohoist": ["**"]
  },
  "dependencies": {
  },
  "devDependencies": {}
}